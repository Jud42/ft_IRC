#include "Server.hpp"

void	Server::stop(void)
{  
	
  //do
	//{
    //int rc;
		//rc = recv(fds[i].fd, buf sizeof(buf), 0);
    //    if (rc < 0)
    //    {
    //        if (errno != EWOULDBLOCK)
    //        {
    //          perror("  recv() failed");
    //          //close_conn = TRUE;
    //        }
  //          break;
	//}
  
}